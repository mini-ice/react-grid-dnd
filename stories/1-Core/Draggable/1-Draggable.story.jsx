import React, { useMemo, useState } from 'react';
import { DndContext, Draggable } from '@/packages';

function DraggableStory({}) {
  const [translate, setTranslate] = useState({ x: 0, y: 0 });

  return <div>1</div>;
}
